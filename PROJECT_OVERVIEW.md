# 공간하우징 프로젝트 개요

## 📋 프로젝트 개요

공간하우징은 건축 회사 공간하우징의 공식 웹사이트입니다. Next.js 15 App Router 기반의 풀스택 웹 애플리케이션으로, Supabase와 Cloudinary를 활용하여 구현되었습니다.

### 프로젝트 목적

- 건축 회사의 시공 사례를 체계적으로 관리하고 공개
- 고객 상담 문의를 효율적으로 처리
- 회사 정보 및 서비스 안내 제공

### 기술적 특징

- **Next.js 15 App Router**: 최신 React 서버 컴포넌트 및 서버 액션 활용
- **TypeScript**: 타입 안정성 보장
- **Supabase**: PostgreSQL 기반 데이터베이스 및 인증 시스템
- **Cloudinary**: 이미지 저장 및 관리
- **Server Actions**: 클라이언트-서버 통신 최적화

### 배포 정보

- **배포 링크**: https://gongganhousing.vercel.app/
- **배포 플랫폼**: Vercel

---

## ✨ 주요 기능

### 1. 시공 사례 관리 시스템 (CRUD)

관리자가 시공 사례를 등록, 수정, 삭제할 수 있는 포트폴리오 관리 시스템입니다.

**핵심 기능**

- 카테고리별 분류: 실내건축, 옥외광고물, 금속창호
- 검색 및 필터링 기능 제공
- 페이지네이션으로 대량 데이터 효율적 표시
- 최대 10개 이미지 업로드 지원
- Zod 스키마를 통한 입력 데이터 검증
- 이미지 미리보기 및 삭제 기능
- 커버 이미지 자동 설정

### 2. 관리자 인증 시스템

Google OAuth를 통한 안전한 관리자 인증 시스템입니다.

**핵심 기능**

- NextAuth.js를 활용한 세션 기반 인증
- 허용된 이메일만 로그인 가능하도록 제한
- Supabase Adapter를 통한 세션 저장
- JWT 토큰 기반 Supabase 접근 권한 부여
- 서버사이드 세션 검증으로 보안 강화
- 관리자만 포트폴리오 등록/수정/삭제 가능

### 3. 상담 문의 시스템

고객이 상담을 요청할 수 있는 문의 폼 시스템입니다.

**핵심 기능**

- 이름, 연락처, 문의 분야, 문의 내용 입력
- 첨부파일 지원 (최대 5개, 총 10MB)
- Cloudflare Turnstile을 통한 봇 차단
- IP 기반 Rate Limiting (10분 내 3회 제한)
- Zod 스키마를 통한 입력 데이터 검증
- Nodemailer를 통한 SMTP 이메일 자동 발송
- 발송 성공/실패 상태 DB 기록

### 4. 이미지 최적화 시스템

클라이언트 측에서 이미지를 자동으로 압축하여 저장 용량과 비용을 절감하는 시스템입니다.

**핵심 기능**

- browser-image-compression 라이브러리 활용
- 최대 0.7MB로 자동 압축
- JPEG 포맷 자동 변환
- 최대 해상도 1200px 제한
- Cloudinary 저장 용량 및 비용 절감

### 5. 데이터 캐싱 시스템

자주 조회되는 데이터를 캐싱하여 성능을 향상시키는 시스템입니다.

**핵심 기능**

- Next.js unstable_cache를 활용한 포트폴리오 목록 캐싱
- React cache 함수를 통한 중복 요청 제거
- 10초 revalidate 주기 설정
- 태그 기반 캐시 무효화
- 데이터베이스 부하 감소 및 응답 속도 개선

### 6. 위치 안내 시스템

카카오맵을 통한 회사 위치 표시 시스템입니다.

**핵심 기능**

- React Kakao Maps SDK 활용
- 회사 위치 마커 표시
- 반응형 지도 제공
- 정보창 표시

---

## 🔧 기술적 해결 방법

### 1. 이미지 업로드 최적화

**문제점**
초기에는 원본 이미지를 그대로 업로드하여 Cloudinary 저장 용량이 빠르게 증가하는 문제가 있었습니다. 대용량 이미지로 인한 느린 업로드 속도와 클라우드 저장 비용 증가가 주요 이슈였습니다.

**해결 방법**
browser-image-compression 라이브러리를 도입하여 클라이언트 측에서 이미지를 사전에 압축하도록 구현했습니다. 이미지를 최대 0.7MB로 압축하고, JPEG 포맷으로 변환하며, 최대 해상도를 1200px로 제한했습니다.

**결과**

- Next.js 서버로 전송되는 요청 크기 **60% 감소**
- Cloudinary 저장 용량 대폭 절감
- 업로드 속도 개선
- 클라우드 비용 최적화 달성

### 2. 이메일 스팸 방지 시스템

**문제점**
상담 문의 폼이 봇 공격 및 스팸 타겟이 될 가능성이 있었고, 무분별한 문의 제출로 인한 서버 부하가 우려되었습니다.

**해결 방법**
2단계 보안 시스템을 구축했습니다. 1단계로 Cloudflare Turnstile을 통해 봇을 차단하고, 2단계로 IP 기반 Rate Limiting을 구현하여 10분 내 같은 IP에서 3회 이상 제출 시 차단하도록 했습니다. 또한 Zod 스키마를 통한 입력 데이터 검증도 함께 적용했습니다.

**결과**

- 봇 공격 효과적 차단
- 동일 IP의 과도한 요청 방지
- 서버 부하 감소
- 안정적인 문의 시스템 구축

### 3. 인증 시스템 개선

**문제점**
초기에는 Supabase Auth를 사용했으나, 빌드 시 모든 라우트가 다이나믹 라우트로 변환되고, 매번 Supabase 클라이언트를 생성해야 하는 등 반복적인 코드가 증가하는 문제가 있었습니다. 또한 서버와 클라이언트에서 인증 상태 관리 방식이 불일치했습니다.

**해결 방법**
NextAuth 세션 기반 인증으로 전환했습니다. Supabase Adapter를 통해 세션을 저장하고, JWT 토큰을 생성하여 Supabase 접근 권한을 부여했습니다. 동시에 Supabase RLS는 그대로 유지하여 데이터베이스 레벨에서의 추가 권한 검증을 보장했습니다.

**결과**

- 서버와 클라이언트에서 통일된 인증 상태 관리
- 코드 간결성 향상
- 정적 라우트 최적화 가능
- 이중 보안 시스템 구축 (NextAuth + RLS)

### 4. 메타데이터 중복 요청 최적화

**문제점**
시공 사례 상세 페이지에서 메타데이터 생성과 페이지 렌더링을 위해 동일한 데이터를 2번 요청하는 문제가 있었습니다. 이로 인해 불필요한 데이터베이스 쿼리가 발생했습니다.

**해결 방법**
React의 cache 함수를 도입하여 동일한 요청을 1회로 통합했습니다. 이를 통해 메타데이터 생성과 페이지 렌더링이 같은 데이터를 공유하도록 구현했습니다.

**결과**

- 동일한 요청을 1회로 통합
- 데이터베이스 쿼리 수 감소
- 페이지 로딩 속도 개선

### 5. 데이터 캐싱을 통한 성능 개선

**문제점**
시공 사례 조회 시 매번 데이터베이스에 직접 요청하여 서버 부하가 증가하고 응답 속도가 느려지는 문제가 있었습니다.

**해결 방법**
Next.js의 unstable_cache를 활용하여 자주 조회되는 포트폴리오 목록 데이터를 캐싱했습니다. 10초 revalidate 주기를 설정하고, 태그 기반 캐시 무효화 시스템을 구축했습니다. 포트폴리오가 생성, 수정, 삭제될 때 해당 태그를 무효화하여 최신 데이터를 보장했습니다.

**결과**

- 시공 사례 목록 및 상세 페이지 로딩 속도 **약 25% 개선**
- 데이터베이스 부하 감소
- 사용자 경험 향상

### 6. 파일 업로드 검증 및 관리

**문제점**
대용량 파일 업로드로 인한 서버 부하와 잘못된 파일 형식 업로드 가능성이 있었습니다.

**해결 방법**
파일 업로드 시 엄격한 검증 로직을 구현했습니다. 개별 파일은 최대 5MB, 전체 파일은 최대 10MB, 파일 개수는 최대 5개로 제한했습니다. 각 검증 단계에서 명확한 에러 메시지를 제공하여 사용자 경험을 개선했습니다.

**결과**

- 서버 부하 예방
- 사용자에게 명확한 에러 메시지 제공
- 안정적인 파일 업로드 시스템 구축

---

## 📊 성능 지표

### 최적화 결과

- 이미지 압축으로 전송 크기 **60% 감소**
- 데이터 캐싱으로 로딩 속도 **25% 개선**
- 불필요한 데이터베이스 쿼리 제거
- 서버 부하 감소

### 기술 스택

| Category  | Tech                                                     |
| --------- | -------------------------------------------------------- |
| Frontend  | Next.js 15, React 19, TypeScript, Tailwind CSS           |
| Backend   | Supabase (PostgreSQL), Cloudinary                        |
| 인증      | NextAuth.js, Google OAuth                                |
| 이메일    | Nodemailer (SMTP)                                        |
| 보안      | Cloudflare Turnstile                                     |
| Libraries | Framer Motion, Embla Carousel, Zod, React Kakao Maps SDK |

---

## 🎯 프로젝트 하이라이트

1. **풀스택 개발 경험**: Next.js 15 App Router를 활용한 서버 컴포넌트 및 서버 액션 기반 풀스택 개발
2. **성능 최적화**: 이미지 압축, 데이터 캐싱 등을 통한 성능 개선 (60% 크기 감소, 25% 속도 개선)
3. **보안 강화**: 다층 보안 시스템 구축 (Turnstile, Rate Limiting, NextAuth, RLS)
4. **비용 최적화**: 클라이언트 측 이미지 압축을 통한 클라우드 저장 비용 절감
5. **사용자 경험**: 반응형 디자인, 직관적인 UI/UX, 빠른 로딩 속도

