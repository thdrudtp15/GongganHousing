import useEmblaCarousel from 'embla-carousel-react';


const ImageDetailModalCarousel = () => {

    const [emblaRef, emblaApi] = useEmblaCarousel();

    return <section ref={emblaRef}>
           
    </section>

}

export default ImageDetailModalCarousel;